[project]
name = "gemma-translate"
version = "0.1.0"
description = "CLI wrapper around Ollama TranslateGemma via the OpenAI client"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=2.26.0",
]

[project.scripts]
gemma-translate = "main:main"
